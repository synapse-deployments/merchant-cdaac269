query run($cartId: ID) { cart(id: $cartId) { cost { subtotalAmount { amount currencyCode } } subtotalPriceV2 { amount currencyCode } } checkout(id: $cartId) { subtotalPriceV2 { amount currencyCode } totalPriceV2 { amount currencyCode } } }